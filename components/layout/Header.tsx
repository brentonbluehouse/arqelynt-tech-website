"use client";
import Image from "next/image";import Link from "next/link";import {ArrowRight,Menu,X} from "lucide-react";import {useEffect,useState} from "react";import {navLinks} from "@/lib/constants";
export function Header(){const[open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>12);f();addEventListener("scroll",f,{passive:true});return()=>removeEventListener("scroll",f)},[]);useEffect(()=>{document.body.style.overflow=open?"hidden":"";const k=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false);addEventListener("keydown",k);return()=>{document.body.style.overflow="";removeEventListener("keydown",k)}},[open]);return <header className={`site-header ${scrolled||open?"solid":""}`}><div className="container nav-wrap"><Link className="brand-logo" href="/" aria-label="Arqelynt Tech LLC home"><Image src="/images/arqelynt-logo.png" width={64} height={64} alt="Arqelynt Tech LLC" priority/></Link><nav className="desktop-nav" aria-label="Primary navigation">{navLinks.map(x=><Link key={x.label} href={x.href}>{x.label}</Link>)}</nav><Link className="button primary header-cta" href="/#contact">Start a Project <ArrowRight size={17}/></Link><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu" aria-label={open?"Close menu":"Open menu"}>{open?<X/>:<Menu/>}</button></div><div id="mobile-menu" className={`mobile-nav ${open?"open":""}`}><nav aria-label="Mobile navigation">{navLinks.map(x=><Link onClick={()=>setOpen(false)} key={x.label} href={x.href}>{x.label}</Link>)}<Link onClick={()=>setOpen(false)} className="button primary" href="/#contact">Start a Project <ArrowRight size={17}/></Link></nav></div></header>}
